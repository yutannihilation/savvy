[package]
name = "unextendr"
version = "0.1.0"
edition = "2021"

[dependencies]
libR-sys = { git = "https://github.com/extendr/libR-sys" }
once_cell = "1"

unextendr-macro = { path = "../unextendr-macro" }

[build-dependencies]
cc = "1"
