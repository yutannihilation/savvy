[package]
name = "savvy"
description = "An unfriendly R extension interface"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
libR-sys = { version = "*", git = "https://github.com/extendr/libR-sys" }
once_cell = "1"

savvy-macro = { version = "*", path = "../savvy-macro" }

[build-dependencies]
cc = "1"
